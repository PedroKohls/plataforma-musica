const { Op } = require("sequelize");
const usuarioModel = require("../models/acorde");
const acordesViolaoModel = require("../models/acordeViolao");

module.exports = {
    store: async function (req, res) {
        
    }
}