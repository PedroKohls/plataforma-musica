<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visualizar Artista</title>
    <link href="/css/menu.css" rel="stylesheet">
    <link href="/css/vizualizarArtista.css" rel="stylesheet">
</head>

<body>
    <header>
        <%- include('menu', locals) %>
    </header>
    <nav class="breadcrumb-container">
        <% breadcrumb.forEach(function(item, index) { %>
            <% if (item.link) { %>
                <a href="<%= item.link %>">
                    <%= item.nome %>
                </a>

            <% } else { %>
                <span class="pagina-atual">
                    <%= item.nome %>
                </span>

                <% } %>
                    <% if (index < breadcrumb.length - 1) { %>
                        <span class="separador">
                            /
                        </span>
                    <% } %>
        <% }) %>
    </nav>

    <div class="container">
        <div class="artista">
            <img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSyW1LFVLrTeSynKd6fZW2JFbbIwM3-jBJilg&s"
                width="250" height="250">
            <h2>
                <%= artista.nome %>
            </h2>
            <div class="options">
                <button>Favoritar</button>
                <button>Gostei</button>
                <button>Não Gostei</button>
            </div>
        </div>

        <div class="conteudo">
            <h1>Músicas mais famosas</h1>
            <div class="musicas">
                <% musicas.forEach(function(musica) { %>
                    <div class="musica">
                        <a href="/musicas/<%= artista.slug %>-<%= artista.id %>/<%= musica.slug %>-<%= musica.id %>">
                            <%= musica.nome %>
                        </a>
                    </div>
                    <% }) %>
            </div>
        </div>
    </div>

</body>

</html>